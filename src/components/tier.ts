// The React tier badge is GENERATED from tokens.json by shadcn/build_shadcn.py — re-exported,
// never copied, so the badge vocabulary cannot drift from the source.
export { Tier, TIERS, type TierId } from "../../shadcn/tier"
